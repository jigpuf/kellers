import React from 'react';

class Exchange extends React.Component {
  render () {
    return (
      <div>
      <ol>All resources come at the expense of other resources.</ol>
      <ol>The goal is to spend as few resources as possible in order to gain
      more important resources.</ol>
      <ol>There is a threshhold where too much focus burns through too many
      other resources</ol>
      </div>
    );
  }
}

export default Exchange;
