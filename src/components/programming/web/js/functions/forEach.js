import React from 'react';

class ForEach extends React.Component {
  render () {
    return (
      <div>
        <h2>For Each</h2>
        let people = ['Bob', 'Bill', 'Kman'];<br />
        Need to finish
      </div>
    );
  }
}

export default ForEach;
