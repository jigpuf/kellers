import React from 'react';

class Forms extends React.Component {
  render () {
    return (
      <div>
      <h2>Forms</h2>
      buttons, text boxes, checkbox, radio buttons, submit buttons, fieldset, 
      </div>
    );
  }
}

export default Forms;
